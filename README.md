# 1StopRemittance

Compare exchange rates across 8+ providers in real-time using the Wise Comparison API.

## Quick Start

npm install && npm run dev

## Deploy to Vercel

Push to GitHub, import in Vercel, add WISE_API_TOKEN env var, deploy.
